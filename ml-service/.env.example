# Bind address/port for the FastAPI service.
HOST=0.0.0.0
PORT=8000

# Origin allowed by CORS — the deployed frontend URL in production.
CLIENT_URL=http://localhost:5173

# Not currently called by any route (reserved for a future ml-service ->
# Node callback); safe to leave as the default.
SERVER_URL=http://localhost:5000
